import 'package:flutter/material.dart';

class MyKadVerificationScreen extends StatelessWidget {
  const MyKadVerificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MyKad Verification')),
      body: const Center(child: Text('MyKad Verification Screen')),
    );
  }
}
